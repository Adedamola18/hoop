# Ralph Progress Log

## Codebase Patterns
- Build verify: swiftc -typecheck -sdk $(xcrun --show-sdk-path) -target arm64-apple-macos14.0 <files>
- Always update project.pbxproj when adding new Swift files (PBXFileReference + PBXBuildFile + PBXGroup + PBXSourcesBuildPhase)
- Use @Observable (Observation framework) not @Published/ObservableObject (macOS 14+)
- LSUIElement agent: Info.plist with LSUIElement=true, no dock icon
- MenuBarExtra + Settings scene pattern for menu bar app
- SMAppService as sole source of truth for launch-at-login state (no UserDefaults)
- SettingsLink is built-in SwiftUI, no need for SettingsAccess SPM package
- NSScreen notch detection: use auxiliaryTopLeftArea/auxiliaryTopRightArea
- Xcode.app is NOT installed — only CLI tools. Use swiftc -typecheck, not xcodebuild build.
- NSTrackingArea with .inVisibleRect auto-updates on view resize — no need to manually recalculate rect
- NotchPanel.notchState is set by NotchWindowManager after panel creation
- SourceKit single-file diagnostics are noise — verify with multi-file swiftc -typecheck
- NSEvent.addGlobalMonitorForEvents fires only for events outside app windows — use for click-outside dismiss
- Panel must be makeKey() to receive keyDown events (Escape key handling)
- ActivationTrigger enum in NotchState.swift with .current static property reads from UserDefaults
- Use Notification.Name.activationTriggerDidChange to broadcast settings changes across components
- Carbon.HIToolbox for key code constants (kVK_ANSI_*) and UCKeyTranslate for display strings
- MediaRemote loaded via dlopen("/System/Library/PrivateFrameworks/MediaRemote.framework/MediaRemote")
- MediaRemote commands: 2=togglePlayPause, 4=next, 5=previous (UInt32)
- MediaService is @Observable — downstream views can observe nowPlaying changes directly
- Services directory: Hoop/Services/ with PBXGroup AA000036
- Widgets directory: Hoop/Views/Widgets/ with PBXGroup AA000037
- MediaService is shared across all panels — single instance owned by NotchWindowManager

Started: 2026-03-10
---

## 2026-03-10 - Pre-Ralph (GSD Phase 1 Plan 01)
- App shell created: Xcode project, LSUIElement agent, MenuBarExtra, settings with launch-at-login
- Files: NotchNookApp.swift, AppDelegate.swift, Info.plist, Views/SettingsView.swift, project.pbxproj, xcscheme
- Commits: 77cde7d (project setup), d871361 (settings window)
- **Learnings:**
  - SettingsLink works within MenuBarExtra on macOS 14+
  - swiftc -typecheck is sufficient build verification without Xcode.app
---

## 2026-03-10 - US-001, US-002 (pre-existing)
- Already implemented from GSD Phase 1 Plan 02
- Verified typecheck passes, marked as passing in PRD
---

## 2026-03-10 - US-003
- Implemented mouse tracking and hover detection for notch expansion
- Files changed: Hoop/Window/NotchPanel.swift, Hoop/Window/NotchWindowManager.swift
- NotchPanel now has NSTrackingArea with mouseEntered/mouseExited
- 200ms dwell timer (configurable via UserDefaults "hoverDwellDelayMs") before triggering expansion
- 300ms grace timer on mouse exit before collapsing
- ignoresMouseEvents set to false (was true) to receive mouse events
- NotchState.phase transitions: idle -> expanding on dwell, expanding/expanded -> idle on grace timeout
- NotchWindowManager wires notchState to panel and installs tracking areas
- **Learnings for future iterations:**
  - NSTrackingArea with .inVisibleRect auto-updates when view resizes
  - installTrackingArea() must be called after setFrame and on reposition
  - SourceKit single-file diagnostics are noise — always verify with multi-file swiftc -typecheck
  - Panel frame snap + SwiftUI spring animation pattern: expand frame first (onExpandRequested callback), then set phase for SwiftUI animation
  - For collapse: set phase first (SwiftUI animates), then delay frame shrink by ~0.5s (onCollapseRequested callback)
  - Use callback closures (onExpandRequested/onCollapseRequested) on NotchPanel to coordinate frame changes with phase transitions
---

## 2026-03-10 - US-004
- Implemented spring animations for expand/collapse transitions
- Files changed: Hoop/Models/NotchState.swift, Hoop/Window/ScreenDetector.swift, Hoop/Window/NotchPanel.swift, Hoop/Window/NotchWindowManager.swift, Hoop/Views/NotchRootView.swift
- NotchState: added collapsedSize, expandedWidth (configurable 400-800pt, default 600pt), expandedHeight (200pt)
- ScreenDetector: added expandedOverlayFrame(expandedWidth:) method
- NotchPanel: added onExpandRequested/onCollapseRequested callback closures; dwell timer expands frame before setting phase, grace timer sets phase then schedules delayed frame collapse
- NotchWindowManager: expandPanel() snaps frame to expanded size, schedules expanding->expanded transition after 0.5s; scheduleCollapsePanel() delays frame shrink by 0.5s for animation; cancel logic prevents conflicts between expand/collapse
- NotchRootView: uses .animation(.spring(response: 0.4, dampingFraction: 0.8)) keyed on isExpanded; shape animates between collapsedSize and full geo size; corner radius animates 10->20
- **Learnings for future iterations:**
  - Panel frame snap + SwiftUI spring pattern avoids coordinating two animation systems
  - Callback closures on NotchPanel keep frame management in the window manager while panel handles mouse events
  - Cancel pending work items when state reverses (e.g., re-hover during collapse delay)
---

## 2026-03-10 - US-005
- Implemented custom NotchShape with concave cutout matching hardware notch profile
- Files created: Hoop/Views/NotchShape.swift
- Files changed: Hoop/Views/NotchRootView.swift, Hoop.xcodeproj/project.pbxproj
- NotchShape is a SwiftUI Shape with animatable cornerRadius, notchWidth, notchDepth
- Concave cutout uses quad Bezier curves at all notch corners for smooth transitions
- On non-notch Macs, falls back to simple RoundedRectangle
- When collapsed (notchDepth=0 or notchWidth >= rect width), draws plain rounded rect
- NotchRootView passes notchDepth=0 when collapsed, collapsedSize.height when expanded for smooth animated cutout appearance
- **Learnings for future iterations:**
  - SwiftUI Shape animatableData with nested AnimatablePair supports multi-property animation
  - NotchShape gracefully degrades: if notch dimensions exceed rect, falls back to rounded rect (handles collapsed state automatically)
  - collapsedSize in NotchState doubles as hardware notch dimensions for the cutout
---

## 2026-03-10 - US-006
- Implemented vibrancy background and dismissal behaviors
- Files created: Hoop/Views/VisualEffectView.swift
- Files changed: Hoop/Views/NotchRootView.swift, Hoop/Window/NotchPanel.swift, Hoop/Window/NotchWindowManager.swift, Hoop.xcodeproj/project.pbxproj
- VisualEffectView: NSViewRepresentable bridging NSVisualEffectView with .hudWindow material and .behindWindow blending
- NotchRootView: ZStack with vibrancy layer (opacity 1 when expanded, 0 when collapsed) and opaque black layer (inverse), both clipped to NotchShape
- NotchPanel: keyDown override for Escape key (keyCode 53) triggers immediate dismiss via onDismissRequested callback
- NotchWindowManager: global click monitor (addGlobalMonitorForEvents) detects clicks outside expanded panel; panel.makeKey() on expand for Escape key reception; immediateCollapse() for instant dismiss with delayed frame shrink
- **Learnings for future iterations:**
  - NSEvent.addGlobalMonitorForEvents only fires for events OUTSIDE the app's windows — perfect for click-outside detection
  - Panel must be made key (makeKey()) to receive keyDown events for Escape
  - Global click monitor should be installed on expand and removed on collapse to avoid unnecessary processing
  - ZStack with dual layers (vibrancy + opaque) and animated opacity is cleaner than trying to animate material changes
---

## 2026-03-10 - US-007
- Implemented activation trigger settings: hover (default), click, keyboard shortcut
- Files changed: Hoop/Models/NotchState.swift, Hoop/Views/SettingsView.swift, Hoop/Window/NotchPanel.swift, Hoop/Window/NotchWindowManager.swift
- Added ActivationTrigger enum (hover/click/hotkey) with UserDefaults persistence
- Settings > General > Activation section with trigger picker
- Hover: shows configurable delay slider (100-500ms, default 200ms)
- Hotkey: shows shortcut recorder (default Option+N), uses Carbon.HIToolbox for key code translation
- NotchPanel.mouseEntered only triggers dwell on .hover; mouseDown override for .click trigger
- NotchWindowManager: global hotkey monitor via NSEvent.addGlobalMonitorForEvents(.keyDown) for .hotkey trigger
- Notification.Name.activationTriggerDidChange broadcast on settings change for immediate behavior update
- **Learnings for future iterations:**
  - Carbon.HIToolbox provides kVK_ANSI_* key code constants for hotkey matching
  - UCKeyTranslate converts key codes to display characters for the shortcut label
  - NSEvent.addGlobalMonitorForEvents(.keyDown) requires Accessibility permission for global hotkeys
  - ActivationTrigger.current static property reads from UserDefaults — no need to pass trigger through the object graph
  - Use Notification for settings changes that affect multiple components (panel + window manager)
---

## 2026-03-10 - US-008
- Implemented MediaRemote private framework service layer
- Files created: Hoop/Services/MediaService.swift
- Files changed: Hoop.xcodeproj/project.pbxproj (added Services group + MediaService.swift)
- MediaServiceProtocol with NowPlayingInfo struct and PlaybackState enum
- MediaRemoteLoader singleton loads MediaRemote.framework via dlopen/dlsym
- Dynamically loaded functions: getNowPlayingInfo, getIsPlaying, sendCommand, register/unregisterForNotifications, getNowPlayingClient
- MediaService (@Observable) implements play/pause/next/previous via MRMediaRemoteSendCommand
- Registers for kMRMediaRemoteNowPlayingInfoDidChangeNotification, isPlayingDidChange, applicationDidChange
- Fetches track title, artist, album art (from artwork data), duration, elapsed time, playback state, and app bundle ID
- getNowPlayingClient uses KVC to extract bundleIdentifier from the opaque client object
- **Learnings for future iterations:**
  - MediaRemote functions use callback pattern with DispatchQueue parameter — always dispatch to main
  - MRMediaRemoteSendCommand takes UInt32 command ID: 2=togglePlayPause, 4=next, 5=previous
  - Album art comes as Data under kMRMediaRemoteNowPlayingInfoArtworkData key, convert to NSImage
  - NowPlayingClient is opaque but responds to KVC for "bundleIdentifier"
  - @convention(c) typealias needed for dlsym function pointers; unsafeBitCast to convert
---

## 2026-03-10 - US-009
- Implemented media player expanded widget view
- Files created: Hoop/Views/Widgets/MediaPlayerWidget.swift
- Files changed: Hoop/Views/NotchRootView.swift, Hoop/Window/NotchWindowManager.swift, Hoop.xcodeproj/project.pbxproj
- MediaPlayerWidget displays album art (80x80 rounded), track title, artist, progress bar, play/pause/next/previous controls
- Shows source app icon via NSWorkspace.shared.urlForApplication(withBundleIdentifier:) + icon(forFile:)
- Widget only renders when playbackState is .playing or .paused (isActive guard)
- NotchRootView now accepts MediaService; shows MediaPlayerWidget when expanded + media active, else "Hoop" text
- NotchWindowManager creates shared MediaService, starts observing on init, passes to all NotchRootView instances
- Added Widgets PBXGroup (AA000037) under Views group
- **Learnings for future iterations:**
  - NSWorkspace.shared.urlForApplication(withBundleIdentifier:) returns app URL for icon lookup
  - Widgets directory lives under Views/Widgets/ with its own PBXGroup
  - MediaService is shared across all panels via NotchWindowManager (single instance)
  - Widget visibility tied to isActive check on playback state — no unnecessary rendering when no media
---
